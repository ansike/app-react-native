import React from 'react';
import {
  SafeAreaView,
  ScrollView,
  StatusBar,
  StyleSheet,
  Text,
  useColorScheme,
  View,
} from 'react-native';

function Orders(props): JSX.Element {
  console.log(props);
  
  return <Text>Orders</Text>;
}

export default Orders;
